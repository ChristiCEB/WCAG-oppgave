<!DOCTYPE html>
<html lang="nb">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' – WCAG vurdering' : 'WCAG vurdering' %></title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header class="site-header">
    <a href="/" class="logo">WCAG vurdering</a>
    <nav class="header-nav" aria-label="Hovednavigasjon">
      <a href="/faq" class="header-link">Hjelp</a>
      <% if (typeof user !== 'undefined' && user) { %>
        <span class="user-status">Logget inn som <strong><%= user.username %></strong></span>
        <form action="/logout" method="post" class="logout-form">
          <button type="submit" class="btn btn-outline">Logg ut</button>
        </form>
      <% } else { %>
        <a href="/login" class="btn btn-outline">Logg inn</a>
        <a href="/register" class="btn btn-primary">Registrer deg</a>
      <% } %>
    </nav>
  </header>
  <main class="main-content">
